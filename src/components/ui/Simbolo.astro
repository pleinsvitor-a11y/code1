---
/** The calligraphic V — the real symbol from the MIV, inlined so it paints with currentColor. */
import { SIMBOLO_D, SIMBOLO_TRANSLATE, SIMBOLO_VIEWBOX } from '@/lib/brand-paths';
interface Props {
  [key: string]: unknown; class?: string; title?: string }
const { class: className = '', title, ...rest } = Astro.props;
---
<svg class={className} {...rest} viewBox={SIMBOLO_VIEWBOX} xmlns="http://www.w3.org/2000/svg" role={title ? 'img' : undefined} aria-hidden={title ? undefined : 'true'} focusable="false">
  {title && <title>{title}</title>}
  <path transform={`translate(${SIMBOLO_TRANSLATE})`} fill="currentColor" d={SIMBOLO_D} />
</svg>
